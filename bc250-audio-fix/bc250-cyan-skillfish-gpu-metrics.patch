diff --git a/drivers/gpu/drm/amd/pm/swsmu/inc/pmfw_if/smu11_driver_if_cyan_skillfish.h b/drivers/gpu/drm/amd/pm/swsmu/inc/pmfw_if/smu11_driver_if_cyan_skillfish.h
index f0561d320f3f..c1f6b91546e7 100644
--- a/drivers/gpu/drm/amd/pm/swsmu/inc/pmfw_if/smu11_driver_if_cyan_skillfish.h
+++ b/drivers/gpu/drm/amd/pm/swsmu/inc/pmfw_if/smu11_driver_if_cyan_skillfish.h
@@ -79,7 +79,8 @@ typedef struct SmuMetrics_t {
 /*
  * The BC-250 normally exposes six cores, which is the published interface
- * above.  Its firmware expands every per-core array when all eight physical
- * cores are enabled, shifting every field that follows those arrays.
+ * above. Its firmware expands the frequency, power, and temperature arrays
+ * when all eight physical cores are enabled, but C0 residency remains at six
+ * entries. The mixed dimensions shift every field that follows those arrays.
  */
 typedef struct SmuMetricsTable8_t {
 	/* CPU status */
@@ -88,7 +88,7 @@ typedef struct SmuMetricsTable8_t {
 	uint16_t CoreTemperature[8];
 	uint16_t L3Frequency[2];
 	uint16_t L3Temperature[2];
-	uint16_t C0Residency[8];
+	uint16_t C0Residency[6];
 
 	/* GFX status */
 	uint16_t GfxclkFrequency;
diff --git a/drivers/gpu/drm/amd/pm/swsmu/smu11/cyan_skillfish_ppt.c b/drivers/gpu/drm/amd/pm/swsmu/smu11/cyan_skillfish_ppt.c
index 8f2f68769604..2dfe26810f71 100644
--- a/drivers/gpu/drm/amd/pm/swsmu/smu11/cyan_skillfish_ppt.c
+++ b/drivers/gpu/drm/amd/pm/swsmu/smu11/cyan_skillfish_ppt.c
@@ -23,6 +23,7 @@
 
 #define SWSMU_CODE_LAYER_L2
 #include <linux/cpu.h>
+#include <linux/delay.h>
 
 #include "amdgpu.h"
 #include "amdgpu_smu.h"
@@ -31,6 +32,8 @@
 #include "smu_v11_8_ppsmc.h"
 #include "smu_v11_8_pmfw.h"
 #include "smu_cmn.h"
+#include "gc/gc_10_1_0_offset.h"
+#include "gc/gc_10_1_0_sh_mask.h"
 #include "soc15_common.h"
 
 /*
@@ -101,6 +104,25 @@ static uint16_t cyan_skillfish_core_count(void)
 	return num_present_cpus() / 2;
 }
 
+static uint16_t cyan_skillfish_get_gpu_activity(struct smu_context *smu)
+{
+	struct amdgpu_device *adev = smu->adev;
+	unsigned int busy = 0;
+	int i;
+
+	/* The firmware table has no GFX activity field. Sample GUI_ACTIVE over
+	 * a short window so gpu_metrics does not expose the 0xffff sentinel.
+	 */
+	for (i = 0; i < 32; i++) {
+		busy += !!(RREG32_SOC15(GC, 0, mmGRBM_STATUS) &
+			  GRBM_STATUS__GUI_ACTIVE_MASK);
+		if (i != 31)
+			udelay(50);
+	}
+
+	return DIV_ROUND_CLOSEST(busy * 10000, 32);
+}
+
 static void cyan_skillfish_expand_metrics_table(SmuMetricsTable8_t *dst,
 						 const SmuMetricsTable_t *src)
 {
@@ -345,6 +367,11 @@ static int cyan_skillfish_read_sensor(struct smu_context *smu,
 						   (uint32_t *)data);
 		*size = 4;
 		break;
+	case AMDGPU_PP_SENSOR_GPU_LOAD:
+		*(uint32_t *)data =
+			cyan_skillfish_get_gpu_activity(smu) / 100;
+		*size = 4;
+		break;
 	default:
 		ret = -EOPNOTSUPP;
 		break;
@@ -491,6 +518,7 @@ static ssize_t cyan_skillfish_get_gpu_metrics(struct smu_context *smu,
 
 	smu_cmn_init_soft_gpu_metrics(gpu_metrics, 2, 2);
 
+	gpu_metrics->average_gfx_activity = cyan_skillfish_get_gpu_activity(smu);
 	gpu_metrics->temperature_gfx = metrics.Current.GfxTemperature;
 	gpu_metrics->temperature_soc = metrics.Current.SocTemperature;
 
